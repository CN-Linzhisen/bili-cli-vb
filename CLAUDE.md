# CLAUDE.md — BiliBili 直播弹幕监控 CLI

## Agent skills

### Issue tracker

Issues live as GitHub issues, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` at the repo root + `docs/adr/` for decisions. See `docs/agents/domain.md`.
